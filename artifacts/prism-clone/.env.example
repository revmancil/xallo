# Optional: absolute origin of the Express API when the UI is hosted separately (e.g. GitHub Pages).
# Do not include a trailing slash or /api (those are added automatically).
# VITE_API_BASE_URL=https://your-api.example.com
